# ci: autoupdate: schedule: quarterly
fail_fast: false
default_install_hook_types: [pre-commit, commit-msg]
default_stages: [pre-commit]

# Generated artifact from repomix; keep stable and ignore.
exclude: ^docs/references/repomix-output-SAFE-MCP-safe-mcp\.xml$

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-toml
      - id: check-added-large-files

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.46.0
    hooks:
      - id: markdownlint-fix
        exclude: "CHANGELOG.md|LICENSE"

  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.23.0
    hooks:
      - id: commitlint
        name: lint commit message for Conventional Commits compliance
        additional_dependencies:
          - "@commitlint/config-conventional"
        stages: [commit-msg]
        args:
          - "--extends=@commitlint/config-conventional"

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks
        name: scan for committed secrets with gitleaks
        args:
          - "--redact"

  - repo: https://github.com/renovatebot/pre-commit-hooks
    rev: 42.38.1
    hooks:
      - id: renovate-config-validator
        name: validate Renovate configuration

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.5
    hooks:
      - id: ruff-check
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format
